import Ember from 'ember';

const {
  Component,
} = Ember;

const MDLLayoutContentComponent = Component.extend({
  classNames: ['mdl-layout__content'],
});

export default MDLLayoutContentComponent;

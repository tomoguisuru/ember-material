import Ember from 'ember';

const {
  Component,
} = Ember;

const MDLCardSupportingTextComponent = Component.extend({
  classNames: ['mdl-card__supporting-text'],
});

export default MDLCardSupportingTextComponent;
